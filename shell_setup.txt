### Setup fish ###
- sudo apt install fish

- Add the fish shell /usr/local/bin/fish to /etc/shells with if it's not there:
> echo /usr/bin/fish | sudo tee -a /etc/shells

- Change your default shell to fish with:
> chsh -s /usr/bin/fish

- install oh-my-fish: https://github.com/oh-my-fish/oh-my-fish 
> curl -L https://get.oh-my.fish | fish
> sudo apt-get install fonts-powerline
> omf install agnoster; omf theme agnoster

### Find Console Colors ###
- https://mayccoll.github.io/Gogh/
  To install: bash -c  "$(wget -qO- https://git.io/vQgMr)" 
  RaspberryPi Bug Workaround: https://github.com/Mayccoll/Gogh/issues/63

### Setup Commmon Aliases ### 
- alias update='sudo apt update ; sudo apt -y upgrade ; sudo apt -y dist-upgrade; sudo apt autoremove -y'

- alias python='python3'

- alias ev='vim ~/.vimrc'

### Cowsay Fortune ###

- Add to bottom: fortune | cowsay


